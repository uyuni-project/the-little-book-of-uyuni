== Overview

In this chapter, you learned:

* {uyuni} can manage clients by using Salt or SSH.
* You can register new clients by using the web UI, bootstrap script, or the direct Salt registration method.
* You can use Activation Key to configure newly registered clients.
* You can manage packages by using the {uyuni} web UI.